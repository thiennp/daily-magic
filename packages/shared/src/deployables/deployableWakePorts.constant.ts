/** AWB loopback HTTP — production-origin Mac install (`~/.agent-witch`). */
export const AGENT_WITCH_BRIDGE_PORT_PRODUCTION_INSTALL = 47892;

/** AWB loopback HTTP — localhost-origin Mac install (`~/.local-agent-witch`). */
export const AGENT_WITCH_BRIDGE_PORT_LOCALHOST_INSTALL = 47893;
