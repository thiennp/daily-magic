# Task composer — known issues

Regression catalog for Send-a-task / live progress. Bridge transport issues live in `src/features/agent-witch/KNOWN_ISSUES.md`.

| ID        | Symptom                                                                                                                                                             | Fix / test                                                                                                                                                                                                                                              |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AGENT-001 | Send-task modal skipped Mac picker when paired devices were still loading, so multi-Mac users never saw selection                                                   | Wait for device load before auto-skipping; `resolveWsTestComposerMacStep.test.ts`                                                                                                                                                                       |
| AGENT-002 | Live terminal mirror stayed empty during writer session start until ready, hiding the local CLI output                                                              | Stream `command.writer.session.chunk`, show pending command while starting                                                                                                                                                                              |
| AGENT-003 | Writer session chunks were dropped when the Mac agent omitted `deviceId` while the session targeted a paired Mac                                                    | Allow executor publish when agent `deviceId` is absent; `npm run agent-witch:test-writer-session-stream`                                                                                                                                                |
| AGENT-004 | `useSendTaskModal` crashed during SSR/Suspense because the Suspense fallback rendered children without context                                                      | Provide a no-op context value in the Suspense fallback; `SendTaskModalProvider`                                                                                                                                                                         |
| AGENT-005 | Claude/Antigravity follow-ups always ran as fresh `-p` one-shots; only Cursor passed `--continue`                                                                   | Enable session continuation for claude-cli + antigravity; `buildWriterCliInvocation.test.ts`                                                                                                                                                            |
| AGENT-006 | Send-a-task showed raw Mac CLI mirror; users needed terminal literacy to follow progress                                                                            | Plain-language progress feed for stepped composer; `buildAgentLiveProgressSteps.test.ts`                                                                                                                                                                |
| AGENT-007 | Progress feed only showed generic labels like “Working on your request…” without agent-authored step detail                                                         | Agents emit `[[PROGRESS]]` blocks; feed renders per-step title + detail; `parseAgentLiveProgressUpdates`                                                                                                                                                |
| AGENT-008 | Suggested next steps after finish sat outside the progress card and were easy to miss in Send-a-task screenshots                                                    | Next-action chips render inside the stepped progress feed; `progress-detail.spec.ts`                                                                                                                                                                    |
| AGENT-009 | Active work step stayed on generic “Working on your request…” until `[[PROGRESS]]`, with no live detail                                                             | Fallback work step shows latest cleaned agent lines as detail; `buildAgentLiveProgressSteps.test.ts`                                                                                                                                                    |
| AGENT-010 | Progress feed looked frozen between Mac updates; waiting-for-answer hid portfolio/`[[PROGRESS]]` findings                                                           | Motion while `isWorking`; merge `partialOutput` into progress steps; `AgentLiveProgressFeed`, `buildAgentLiveProgressSteps`                                                                                                                             |
| AGENT-011 | Dispatch/system errors wiped the live task session, forcing a full restart                                                                                          | Keep session on `error`, persist to localStorage, clear only when finished; `agentLiveTerminalLocalStore.test.ts`                                                                                                                                       |
| AGENT-012 | Stepped Send-a-task hid the Mac PTY when showing plain-language progress                                                                                            | Show progress feed and local terminal mirror together; `AgentLiveTerminalPanel`                                                                                                                                                                         |
| AGENT-013 | Send-a-task Mac picker labeled recently-seen (not live) Macs as **Online**                                                                                          | Use `formatMacPresenceStatusLabel` / presence tiers; `macDevicePresence.test.ts` (AGENT-013)                                                                                                                                                            |
| AGENT-014 | Live Mac PTY replaced “Local Mac terminal”, so the selected terminal panel stopped updating during a run                                                            | Keep PTY inside Local Mac terminal chrome; mirror `shell.data`; `shouldShowLiveMacShellInTerminal.test.ts`                                                                                                                                              |
| AGENT-015 | Owner saw “Only the Mac owner can control this shell” on resize/input across Next vs `server.ts` bundles                                                            | Share shell session maps on `globalThis`; distinct unknown-session error; `shellSessionRegistry.test.ts`                                                                                                                                                |
| AGENT-016 | Offline Mac errors said “No Mac is connected for your account” on gated Agent screens that already require a pair                                                   | Say Mac is offline + open Agent Witch; `MAC_OFFLINE_FOR_ACCOUNT_ERROR`; hub + session error tests (AGENT-016)                                                                                                                                           |
| AGENT-023 | Send-a-task kept a revoked Mac id in live-terminal localStorage, so retries targeted a dead device while Home showed the live Mac online                            | `resolveAgentSessionTargets` only locks `sessionDeviceId` when it is still in the paired device list; `resolveAgentSessionTargets.test.ts`                                                                                                              |
| AGENT-024 | Local Mac terminal stayed empty and “Working on your request” never finished after Start; local traffic showed `command.claude.run` then reconnect, no `shell.data` | `node-pty` threw `posix_spawnp failed` and crashed Agent Witch; catch spawn errors and fall back to pipe spawn; install bundle **25**                                                                                                                   |
| AGENT-025 | After pipe fallback / remount, Send-a-task stayed on “Waiting for the first update…” with an empty **Live** PTY even when the job had finished                      | Persist streaming mirror output; hydrate from run output store; finish on completed `agent.run.record`; prefer stream mirror over empty Live PTY; `agentLiveTerminalLocalStore.test.ts`, `shouldShowLiveMacShellInTerminal.test.ts`                     |
| AGENT-026 | Finished jobs could not be brought back with Mac CLI context; next-action links started a fresh thread                                                              | Keep finished live sessions restorable; `continueSession=1` + thread-already-started mark `--continue`; Continue conversation on job detail; `resolveIsWriterSessionContinuation.test.ts`                                                               |
| AGENT-027 | Send-a-task asked for Mac and CLI every open even after the user had already chosen them                                                                            | Persist last Mac + CLI in localStorage; auto-complete those steps when still valid; trail rewind still lets the user change; `preferredMacDeviceStorage.test.ts`, `resolveWsTestComposerWriterAgentCompleted.test.ts` (AGENT-027)                       |
| AGENT-028 | Custom task picked a CLI and immediately started a Mac session, skipping the “describe what should run” prompt form                                                 | Always complete the writer step into the form; Start alone begins the session; `shouldStartWriterAgentOnCliSelect.test.ts` (AGENT-028)                                                                                                                  |
| AGENT-029 | Start jumped straight into a Mac run with no way to pick a workflow or continue a past job                                                                          | Picker lists recent history + library workflows; finished sessions no longer block the wizard; history sets `continueSession`; `resolveIsSendTaskSessionActive.test.ts`, `buildSendTaskComposerHistoryPickerItems.test.ts` (AGENT-029)                  |
| AGENT-030 | Navigating away from Send-a-task tore down the live panel instead of docking it                                                                                     | Root-mounted provider + expanded/minimized presentation; sticky bottom-right dock keeps `WsTestPanel` mounted; `resolveSendTaskPresentation.test.ts` (AGENT-030)                                                                                        |
| AGENT-031 | Expanded Send-a-task overlay (`z-99999`) covered the header, so Library/Home clicks could not dock the panel                                                        | Modal shell uses `z-40` under header/bottom nav (`z-50`); navigating away docks; Expand restores `?sendTask=1` on the current path                                                                                                                      |
| AGENT-032 | Start reopened a prior Custom-task live panel instead of the workflow picker                                                                                        | Only restore persisted live terminal on `continueSession` / `resumeLive`; Start remounts a fresh panel; Expand uses `resumeLive=1`; `resolveShouldRestoreAgentLiveTerminalSession.test.ts` (AGENT-032)                                                  |
| AGENT-033 | Continue-from-history and job history had no way to remove past runs                                                                                                | `deleteAgentRunHistory` / `clearAgentRunHistory` + `DELETE /api/agent-runs/[runId]`; picker trash + Clear history; job card Delete; `agentRunLocalCache.test.ts`, `deleteAgentRunHistory.test.ts` (AGENT-033)                                           |
| AGENT-034 | Clear/delete left Continue-from-history intact when server delete failed or only local ids were cleared                                                             | Always drop local + tombstone first; clear fetches `/api/agent-runs` and deletes every id; Job history Clear all; `deleteAgentRunHistory.test.ts` (AGENT-034)                                                                                           |
| AGENT-035 | Send-a-task listed duplicate Freelancer client proposal rows with no way to remove them from the picker                                                             | Trash on library rows archives via `DELETE /api/capabilities/[id]` and drops local library state; `deleteSendTaskComposerLibraryItem.test.ts` (AGENT-035)                                                                                               |
| AGENT-036 | Clicking the selected LLM CLI in the step trail did nothing unless the tiny pencil icon was hit; re-picking the highlighted CLI after rewind was unclear            | Make the full trail row a button again; show `aria-pressed` on the current writer row; `resolveWriterAgentWizardAdvanceOnSelect.test.ts` (AGENT-036)                                                                                                    |
| AGENT-037 | Closing Send-a-task or navigating away during a live Mac run unmounted `WsTestPanel` and dropped the Agent Witch socket                                             | Close docks while `resolveIsSendTaskSessionActive`; URL leave sets keep-alive; `resolveSendTaskCloseAction.test.ts` in `resolveSendTaskPresentation.test.ts` (AGENT-037)                                                                                |
| AGENT-038 | Send-a-task stayed on “Waiting for the first update…” with no hint when the Mac never streamed output                                                               | Client stall detection: 30s warning, 90s stuck banner + work-step detail; `resolveAgentLiveProgressStallState.test.ts` (AGENT-038)                                                                                                                      |
| AGENT-042 | Progress feed only showed the latest user answer; earlier checkpoint questions and replies were lost from the step list                                             | Persist `[[CHECKPOINT_QA]]` records on answer submit; interleave Agent asked / Your answer steps with `[[PROGRESS]]`; `buildAgentLiveProgressSteps.checkpoint.test.ts` (AGENT-042)                                                                      |
| AGENT-043 | Continue-from-history let users pick a different Mac, so Mac CLI `--continue` resumed the wrong local thread                                                        | Pin `deviceId` on continue links; hide history rows without a saved Mac; server rejects `sessionContinuation` without `targetDeviceId`; `canContinueAgentRunOnStoredMac.test.ts`, `validateSessionContinuationRequiresTargetDevice.test.ts` (AGENT-043) |
| AGENT-044 | Job-history continue omitted Mac/writer/run scope and lost prior context when the CLI thread was cold                                                               | `sourceRunId` + `buildAgentRunContinueHref`; restore from `agentRunLocalCache`; Mac seeds via `buildContinuationPromptWithContext` when `--continue` unavailable; `buildAgentRunContinueHref.test.ts` (AGENT-044)                                       |
