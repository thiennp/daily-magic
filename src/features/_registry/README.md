# Feature registry

Canonical list of product features for folder layout, docs, and RAG indexing.

- **Data:** `features.registry.json`
- **Types:** `featureRegistry.types.ts`
- **Loader:** `features.registry.ts`

When adding a feature, update the JSON entry and run:

```bash
npm run feature-knowledge:scaffold-docs
npm run feature-knowledge:index
```
