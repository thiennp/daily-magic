/**
 * Bump when shipping user-visible server fixes so production can prove deploy
 * without opening Railway (surfaced on GET /api/health and the in-app badge).
 */
export const AGENT_WITCH_SERVER_RELEASE_LABEL = "2026-09-16-dispatch-retarget";
