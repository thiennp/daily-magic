/** Mac is not on a live Agent Witch WebSocket (any hub instance). */
export const MARKETPLACE_INSTALL_MAC_NOT_LIVE_ERROR =
  "This Mac is not connected for install. Open Agent Witch on that Mac, then try Install again.";

/** Heartbeat is fresh but no live WS — matches marketplace install modal copy. */
export const MARKETPLACE_INSTALL_MAC_RECENT_NOT_LIVE_ERROR =
  "This Mac checked in recently but does not have a live Agent Witch connection. Open Agent Witch on that Mac, then try Install again.";
