/** Soft `[[WORKING_ESTIMATE]]` — guidance only; runs may continue past it. */
export const AGENT_LIVE_SOFT_ESTIMATE_LABEL = "Soft estimate (guidance)";

/** Pre-warn before a provider **hard stop** (distinct from soft estimate). */
export const AGENT_LIVE_SESSION_LIMIT_PREWARN_LABEL = "Session limit warning";

/** Terminal / Reports label when the hard stop already happened. */
export const AGENT_LIVE_HARD_STOP_SESSION_LIMIT_LABEL =
  "Hard stop: session limit";
