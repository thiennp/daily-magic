/**
 * Primary nav destinations shown only when the user belongs to at least one company group.
 * Marketplace stays visible for solo users — they browse/install shared harness sets there too.
 */
export const SHELL_NAV_TEAM_ONLY_HREFS: readonly string[] = ["/automations"];

export const SHELL_NAV_ADMIN_HREF = "/admin/groups";
