/** Primary nav destinations shown only when the user belongs to at least one company group. */
export const SHELL_NAV_TEAM_ONLY_HREFS: readonly string[] = [
  "/marketplace",
  "/automations",
];

export const SHELL_NAV_ADMIN_HREF = "/admin/groups";
