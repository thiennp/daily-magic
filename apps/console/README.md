# AWC — Agent Witch Console

Cloud control plane (`www.agentwitch.com`, `localhost:3000`).

**Today:** `src/app/`, `server.ts`, product features under `src/features/`, shared server under `src/lib/`.

**Target:** `apps/console/` with its own `features/` tree per [FSA](../docs/architecture/fsa-refactoring-plan.md).
