/** Default scan folder shown in the local harness UI (`~` expands under home). */
export const DEFAULT_LOCAL_HARNESS_SCAN_FOLDER = "~";

export const buildDefaultLocalHarnessScanFolder = (): string =>
  DEFAULT_LOCAL_HARNESS_SCAN_FOLDER;
