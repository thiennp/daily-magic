# AWL — Agent Witch Live

Mac-local web app at `http://local.agentwitch.com:43347` (`127.0.0.1:43347`).

**Today:** `scripts/agentWitchLocalApp.ts`, `scripts/buildAgentWitchLocal*`, `scripts/localHarness/*`.

**Target:** `apps/live/features/` (FSA slices) + `apps/live/entry/` — see [FSA.md](./FSA.md) and [docs/architecture/awl-fsa-plan.md](../../docs/architecture/awl-fsa-plan.md).

**Registry:** [features.registry.json](./features.registry.json)

**Shared:** `@agent-witch/shared` (live port/origins) · `@agent-witch/install-layout` (profile paths, when slices need disk layout).
