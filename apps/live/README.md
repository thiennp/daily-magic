# AWL — Agent Witch Live

Mac-local web app at `http://local.agentwitch.com:43347` (`127.0.0.1:43347`).

**Today:** `scripts/agentWitchLocalApp.ts`, `scripts/buildAgentWitchLocal*`, `scripts/agentWitchLocalApp.constants.ts`.

**Target:** `apps/live/features/` (presentation + local HTTP routes).
